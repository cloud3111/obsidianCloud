---
banner: "[[pixel-banner-image.png]]"
title: 深度学习与智能体(Agent)技术
tags:
  - Agent
  - Model
  - Rag
categories: 编程
date: 2026-04-04T18:59:00
---

# 深度学习与智能体(Agent)技术
![17753022760231775302275598.png|700x498](https://fastly.jsdelivr.net/gh/cloud3111/cloudWallpaper@main/17753022760231775302275598.png)
[参考文献](https://cloud.tencent.com/developer/article/2561903)

	什么都无法舍弃的人，什么都无法改变
## 计算机视觉模型训练精要
## 数据集
训练集格式: 图像和标签
voc格式 每一个图像对应一个xml文件
yolo格式 每一个图像对应一个txt文件
如果有时候还需要通过脚本生成图像和标签的索引txt文件: 用来区分训练集和验证集(通常是8:2)
图像标注: 将图像说所包含的结果通过人工行为标注出来(可以通过标注工具，例如labelimg生成xml或者txt文件)
当然也可以通过下载VOC12+07的数据集，但是如果要通过微调来指针具体的业务场景进行业务训练，像我们之前那个公司是做无人值守便利店，接入了视觉模型对具体的顾客拿烟行为进行判定，具体的训练数据也是外包出去通过人工标注来获得的，以此达到模型微调的目的，因为大模型很笨，所以只有通过大量的数据训练来进行进化，不单单是视觉模型，ai对话模型像ChatGPT，deepseek也是通过大量数据来训练的，为什么近些年ai好像不像前些年那样进步飞快了，原因很简单，训练数据都是基于人类本身的，现存的网络数据库的，当某一天模型的训练速度大于数据的生成速度时，自然而然就进步缓慢了，如果要解决这个问题，就要虚构一些不存在的数据，这就会导致另一个问题的发生: 幻觉！
## 底层模型选择
通过选用不同的模型来获取不同的识别效果
yolov8 目标检测模型
papnet 图像语义分割模型
## 开始模型训练
参数
是否使用CUDA
选择主干网络
输入图像大小(输入尺寸)
每次训练数量BatchSize
Epoch循环次数
是否使用权重文件pth(model_path)
区分类个数(numble_classes) 重要
工作线程数(workers)
## 模型验证阶段
数据集划分: 训练集，验证集和测试集
数据集格式检验
问：为什么要冻结训练和解冻训练呀？ 答：这是迁移学习的思想，因为网络主干特征提取部分所提取到的特征是通用的，我们冻结起来训练可以加快训练效率，也可以防止权值被破坏。 在冻结阶段，模型的主干被冻结了，特征提取网络不发生改变。占用的显存较小，仅对网络进行微调。 在解冻阶段，模型的主干不被冻结了，特征提取网络会发生改变。占用的显存较大，网络所有的参数都会发生改变
## 指标
判断损失函数是否有收敛的趋势
判断是否过拟合(在训练集上表现良好，但在验证集没出现过的数据上准确率低)
判断精确率
判断召回率
## 大语言模型（LLM）和视觉模型（VM）和智能体
Large Langueage Model
前者是基于toekn进行上下文推测生成回答
后者是基于图片/视频进行图像分类，语义分割，目标检测
智能体：基于Python/Java语言对大语言模型或者视觉模型进行赋能，让其在回复的基础上调用工具，比如购票：能实现自主查余票，购票下单等流程
SpringAi：用Java语言基于大语言模型构建智能体的框架
## Agent开发工程师核心工作
1. Prompt设计
2. Tool Calling 工具调用
3. Rag检索中间库
4. Memory上下文对话历史
结合以上功能的才能算作一个针对某个方向的智能体, 后面可以升级为多Agent协同
## Prompt
让一位说书人(角色)，用生动的语气(风格语气)，给孩子们(受众)讲个故事(指令)。故事的开头(上下文是…结局(目标)要感人。故事的结构(格式)要像这样(示例)，但不要(约束)出现暴力情节。请先构思情节(逐步思考)，写完后再想想怎么能更精彩(反思)
## Rag
RAG（检索增强生成）：为LLM注入知识与记忆
定位：解决LLM的“静态知识”和“幻觉”问题的核心技术。
核心流程：
索引：将私有知识库（文档、手册、代码等）进行分块、向量化，并存入向量数据库。
检索：根据用户查询，从向量库中快速检索出最相关的知识片段。
增强：将检索到的片段作为上下文，与用户查询一同提交给LLM。
生成：LLM基于提供的权威上下文生成更准确、可靠的答案
原则上可以利用所以现存的数据库, 包括关系型和非关系型数据库来做,  但是比较常用的比较适配的有PostgreSQL,PgVector  在我自己的项目中使用的是ES作为向量数据库
## ToolCallingAndMCP
用户问题
 ↓
LLM 推理
 ↓
决定：调用某个 Tool
 ↓
生成 Tool 参数（arguments）
 ↓
Spring AI 调用你写的 Java 方法
 ↓
方法里访问 DB / HTTP / 搜索引擎
 ↓
结果再喂给 LLM