# 2025-09-11

## 刷题 力扣hot100

### 1. 两数之和
- 为什么使用哈希表
	- 当遇到查询数组的一个元素是否在另一个数组中出现, 就要使用哈希表
- 明确map中的key和value用来存什么的
	- **key来存元素，value来存下标**
- 难点  用边查边插入的方式  而不是说先把所有值都映射到哈希表
	- 这样能保证不会查到自己

### 49. 字母异位词分组
- 将字符串数组 排序之后再分组, 可以考虑用HashMap, key是排序之后的词, value为排序前的词
- 每次以map中是否存在排序之后的词为条件

### 128. 最长连续序列
- 找连续序列的题
	- 是否可以排序
	- 找到序列头
	- 考虑用Set保存所有值, 比较前值后值是否存在
	- 在脑海里描述动图: 前指针不动for, 后指针while循环
- 难点  直接对Set做遍历, 不用去重  而且只需要用Set.contains做判断

### 15. 三数之和
- 是否可以排序
- 3指针:  当前下标, i+1为左指针, length-1为右指针
- **根据3指针之和 大于或者小于0 收缩左/右窗口while** 
- 难点  没去重
	- 在每次sum == 0 时while判断下一个值是否与前一个值相同, 相同则跳过
	- 判断i和i-1的值是否相同, 相同则跳过 

### 3. 无重复字符的最长子串
- 用滑动窗口, 左指针和右指针从0开始, 右指针负责扩大窗口, 左指针负责缩小
### 438. 找到字符串中所有字母异位词

### 560. 和为k的子数组
- 暴力枚举
### 53. 最大子数组和

### 206. 反转链表
- 如果cur.next在右边代表的是元素本身, 在左边代表指向
### 234. 回文链表
- 将值复制到数组中用双指针
### 2. 两数相加
- 注意: 这里的进位顺序反了  **逆序存储**
### 53. 最大子数组和
- 利用前缀和缩小扩大窗口
- 难点: minPreSum=维护当前遍历过的所有前缀和中的最小值  因为求最大的前提是减最小窗口