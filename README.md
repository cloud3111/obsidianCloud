<<<<<<< HEAD
<<<<<<< HEAD
# hexo博客网站

- Hexo 是一个基于 Node.js 的静态博客框架，主要用于生成和部署静态博客

## **🚀 1. 安装 Hexo**

Hexo 依赖 Node.js 和 npm，确保已安装：

```
node -v
npm -v
```

如果没有安装，去 [Node.js 官网](https://nodejs.org/) 下载并安装。

### **安装 Hexo CLI**

```
npm install -g hexo-cli
```

安装完成后，验证：

```
hexo -v
```

如果无法运行 `hexo`，请检查是否已添加 `npm` 全局路径到 `PATH`（见上面解决方案）。

------

## **📂 2. 初始化 Hexo 博客**

### **创建一个新的 Hexo 项目**

```
hexo init my-blog
cd my-blog
npm install  # 安装依赖
```

这样就创建了 `my-blog` 目录，里面包含 Hexo 的基本文件结构：

```
bash复制编辑my-blog/
  ├── _config.yml   # 站点配置文件
  ├── package.json  # 依赖管理
  ├── scaffolds/    # 文章模板
  ├── source/       # 博客内容（文章、页面等）
  ├── themes/       # 主题目录
  └── public/       # 生成的静态文件（hexo g 后出现）
```

------

## **🔥 3. 运行 Hexo**

### **本地预览**

```
hexo server # 或者 hexo s
```

打开浏览器，访问 `http://localhost:4000/`，你就能看到 Hexo 的默认博客页面。

------

## **✏️ 4. 写文章**

### **新建文章**

```
hexo new "我的第一篇博客"
```

文章会出现在 `source/_posts/` 目录下，例如：

```
source/_posts/我的第一篇博客.md
```

打开 Markdown 文件，编辑内容。

### **发布文章**

1. 生成静态文件：

   ```
   hexo generate 或者 hexo g
   ```

   生成的静态文件在 `public/` 目录下。

2. 部署到 GitHub Pages：

   ```
   hexo deploy 或者 hexo d
   ```

   需要配置 `deploy`，例如 GitHub Pages：

   ```
   deploy:
     type: git
     repo: https://github.com/你的用户名/你的仓库.git
     branch: gh-pages
   ```

   并安装部署插件：

   ```
   npm install hexo-deployer-git --save
   ```

------

## **🎨 5. 更换主题**

Hexo 主题可以在 Hexo 主题官网 查找。

### **安装主题**

例如安装 `Butterfly` 主题：

```
git clone -b master https://github.com/jerryc127/hexo-theme-butterfly.git themes/butterfly
```

然后修改 `_config.yml`：

```
theme: butterfly
```

再重新生成静态文件：

```
hexo clean && hexo g
```

------

## **📌 6. 常用命令**

| 命令                        | 作用                           |
| --------------------------- | ------------------------------ |
| `hexo init <folder>`        | 初始化 Hexo 博客               |
| `hexo new <title>`          | 新建文章                       |
| `hexo g` 或 `hexo generate` | 生成静态文件                   |
| `hexo s` 或 `hexo server`   | 本地启动 Hexo 服务器           |
| `hexo d` 或 `hexo deploy`   | 部署到远程仓库                 |
| `hexo clean`                | 清理缓存（解决一些问题时有用） |

------

## **📚 7. 进阶学习**

- Hexo 官方文档
- Hexo + GitHub Pages 搭建个人博客
- Hexo 主题定制
=======
write a md to descript obsidian cloud repository
>>>>>>> origin/main
=======

>>>>>>> origin/main
